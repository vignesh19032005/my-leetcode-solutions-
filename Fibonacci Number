class Solution:
    def fib(self, n: int) -> int:
        def fib_num(num):    
            if num <= 1:
                return num
            else:
                return fib_num(num - 1) + fib_num(num - 2)
        
        return fib_num(n)
